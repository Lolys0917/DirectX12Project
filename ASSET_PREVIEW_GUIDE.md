# 再生設定とアセットプレビュー

## 再生タブ

Start / Pause / Stop / Tickと更新FPS（1〜240）に加え、以下の値を調整できます。

| 設定 | 初期値 | 用途 |
|---|---:|---|
| ゲーム速度 | 1 | 更新に渡す経過時間の倍率。0で更新停止、0.5で半速、2で倍速。Tickにも適用 |
| 基礎重力 XYZ | 0, -9.81, 0 | Gravityスクリプトに渡す加速度 |
| 空気抵抗 | 0.1 | 速度の減衰 |
| 反発係数 | 0.3 | 簡易床との衝突時の反発。0〜1 |
| 床の高さ | 0 | 簡易床のY座標 |
| 最高速度 | 55 | Gravityスクリプトの速度上限 |
| カメラ速度 | 5 | 標準カメラの移動速度。コードで設定した速度には倍率として適用 |
| 背景の向き | 0° | 背景の水平回転 |
| 背景の明るさ | 1 | 背景色の倍率 |

数値は「設定を適用・保存」で反映し、`Assets/EditorSettings.ini`に保存します。FPSはその場で反映されますが、次回起動にも残す場合は同じ保存ボタンを押してください。無効な数値は設定全体を変更せずに拒否します。「初期値に戻す」は数値・FPSを初期値に戻します。

重力を使うにはEngineタブで対象Objectを選び、Script追加から「重力 / Gravity」を選択します。すべての物体が勝手に落下することはありません。Objectごとに`GravityScale`、床から原点までの距離`GroundOffset`を変更できます。`ResetVelocity`は速度をゼロにします。

床と重力は**Local座標による簡易シミュレーション**です。物体同士の衝突や、回転・拡縮した親を考慮する本格的な剛体シミュレーションではありません。基本的には親を持たないObjectで使用してください。Stopは従来どおり再生開始前のSceneへ戻します。設定値はStopで戻りません。

## プレビュータブ

1. 「ファイル読込」で画像・OBJモデルを選択します。複数選択もできます。
2. ファイルを`Assets/Textures`または`Assets/Models`へコピーします。同名ファイルは上書きせず、連番を付けます。
3. 一覧で選んだアセットを専用の描画領域へ表示します。ゲーム画面や再生状態は変更しません。
4. モデルは左右・上下の矢印で15°ずつ回転し、＋／−で拡大縮小、「正面」でリセットできます。
5. 「モデルに画像…」でプレビュー中のOBJへ画像を貼れます。この操作はプレビュー専用です。

ファイル名検索、一覧の更新、保存場所を開く操作、Assets内の名前変更も用意しています。シーンが参照中のファイル、使用中の背景、再生／一時停止中のファイルの名前変更は参照を保護するため拒否します。ファイル削除は行いません。

初回の画像転送やOBJ読み込み中は「読み込み中…」を表示します。失敗した場合は直前のプレビューを残します。画像は縦横比を維持し、透明部分はチェック柄で表示します。

## シーンへの適用

- **画像をモデル等へ貼る:** Engineタブで対象Objectを選択 → プレビュータブで画像を選択 → 「選択Objectに貼付」。OBJ、Box等のPrimitive、MeshComponentが対象です。複数のモデル／メッシュComponentがある場合は最初の対応Componentへ適用します。
- **モデルを追加:** OBJを選び「シーンへ追加」。サイズを自動調整し、原点付近へ追加します。OBJの原点・元サイズは読み込みデータに保持され、ObjectのTransformで見やすく配置します。
- **背景を変更:** 画像を選び「背景に使用」。カメラから見た方向に応じて画像を全天へ投影します。「背景を解除」で解除できます。背景の選択は保存されます。

ユーザー提供の`joran-quinten-CRmulUkILVg-unsplash.dds`を`Assets/Textures`へコピーし、初期背景として設定しています。元のDownloadsファイルは変更しません。この画像は3000×1998の通常写真なので、全天投影では継ぎ目・歪みが生じます。背景専用には横長2:1の全天球画像を使用すると自然になります。

シーンへ追加したモデル・貼付テクスチャの保存は既存のScene管理と同じ扱いです。この変更ではScene全体のファイル保存機能は追加していません。アセットファイル、再生設定、背景設定は再起動後も残ります。

## 対応形式と制限

- 画像: DDS、PNG、JPEG、BMP、TIFF（DDS以外はWindows WICで読み込み）
- DDS: 2DのDXT1/BC1、DXT3/BC2、DXT5/BC3、RGB24、RGBA/BGRA8。対応するDX10拡張ヘッダーも読み込めます。先頭ミップをRGBAへ展開し、端数寸法を保持します。
- BC4〜BC7、HDR、キューブマップDDS、配列、ボリュームDDSは未対応です。キューブマップDDSそのものを使う方式ではなく、2D画像からのスカイボックス相当の背景描画です。
- モデル: Wavefront OBJ。FBX/glTF、アニメーション、MTLの自動読み込みは未対応です。画像は個別に指定してください。
- 画像は最大16384×16384かつ67,108,864画素、DDS入力ファイルは256MiBまでです。

DDSの構造とアップロード処理は[Microsoft DDSガイド](https://learn.microsoft.com/en-us/windows/win32/direct3ddds/dx-graphics-dds-pguide)と[Direct3D 12テクスチャ転送資料](https://learn.microsoft.com/en-us/windows/win32/direct3d12/upload-and-readback-of-texture-data)を参照しています。

## 検証

`Tests/DDSImageTests.vcxproj`をDebug x64でビルドし、リポジトリ直下から`Tests/.output/DDSImageTests.exe`を実行すると、端数サイズ・透明度・破損ヘッダー・不足ミップ・実際の提供DDS・設定範囲を検証できます。実行時のEngineDiagnosticsには、DDSのGPU転送、Primitive/OBJへの適用、複製、背景描画、基礎重力変更と床判定の検証を追加しています。
